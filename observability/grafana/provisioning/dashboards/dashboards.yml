apiVersion: 1

providers:
  - name: postgres-ha-chaos-lab
    orgId: 1
    folder: "Postgres HA Chaos Lab"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
